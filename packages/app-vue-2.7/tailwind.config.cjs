const ankorStoreDs = require("@ankorstore/design-system/tailwind.config.js");

module.exports = {
  mode: "jit",
  presets: [ankorStoreDs],
  darkMode: false,
  theme: {
    extend: {},
  },
  variants: {
    extend: {},
  },
  plugins: [],
};
